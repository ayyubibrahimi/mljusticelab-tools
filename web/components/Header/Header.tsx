import React from 'react';
import './Header.module.scss';

const Header: React.FC = () => {
  return (
    <header className="header">
      <h1 className="headerTitle">Innocence Discovery Lab</h1>
      <nav className="headerNav">
      </nav>
    </header>
  );
};

export default Header;